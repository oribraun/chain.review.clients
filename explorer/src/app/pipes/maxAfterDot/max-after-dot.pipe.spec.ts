import { MaxAfterDotPipe } from './max-after-dot.pipe';

describe('MaxAfterDotPipe', () => {
  it('create an instance', () => {
    const pipe = new MaxAfterDotPipe();
    expect(pipe).toBeTruthy();
  });
});
